# Core (aktuell gebraucht)
gymnasium==0.28.1               # Environment Framework (Ray 2.30 requirement)
PyYAML==6.0.1                   # Config Files
numpy==1.26.4                   # Berechnungen

# Multi-Agent RL Training
ray[rllib]==2.30.0              # RLlib fuer Multi-Agent Training (Python 3.11 compatible)
torch==2.1.0                    # Deep Learning Backend

# Fix PyArrow version for Python 3.11
pyarrow>=12.0.0                 # Modern PyArrow with pre-built wheels for Python 3.11

# Visualization (spaeter fuer Frontend)
# matplotlib==3.8.0
# plotly==5.18.0

# Web (spaeter fuer Backend/Frontend)
# streamlit==1.29.0             # Frontend
# fastapi==0.109.0              # Backend API
# uvicorn[standard]==0.27.0     # Backend Server
# requests==2.31.0              # HTTP Client

# Cloud (spaeter fuer Deployment)
# gcsfs==2023.12.0
# google-cloud-storage==2.14.0
# google-cloud-pubsub==2.18.4
